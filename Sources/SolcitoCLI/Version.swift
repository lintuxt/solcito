enum Version {
    /// Bumped manually at release time. Keep in sync with the git tag
    /// (the release workflow attaches an asset named
    /// `solcito-vX.Y.Z-macos-arm64.tar.gz` based on the tag).
    static let current = "0.1.2"
}
